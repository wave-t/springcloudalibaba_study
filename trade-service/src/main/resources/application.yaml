server:
  port: 8084
feign:
  okhttp:
    enabled: true #开启okhttp功能
  sentinel:
    enabled: true #开启sentinel功能

# Nacos共享配置变量参数
hm:
  swagger:
    title: 订单服务接口文档
    package: com.wave.trade.controller
  db:
    database: hm-trade
  mq:
    virtual: hmall
    name: hmall
    pwd: 123